{
  "name": "ainigma-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "AInigma demo backend — simulated multi-agent pipeline + optional live pass (free models) + MCP inventory server.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "mcp": "tsx src/mcp/inventoryServer.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@modelcontextprotocol/sdk": "^1.0.4",
    "fastify": "^4.28.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "tsx": "^4.16.2",
    "typescript": "^5.6.2"
  }
}
